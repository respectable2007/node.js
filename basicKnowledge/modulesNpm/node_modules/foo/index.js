// 模块对象的属性
// 在模块文件内部，可以访问以下属性

// module.id：当前模块的ID。默认情况下，主模块的ID为"."，
// 其他模块的ID为该模块文件的绝对路径。可在模块文件中修改当前
// 模块的ID
// console.log(module.id)
// module.id='foo'
// console.log(module.id)

// module.filename：当前模块文件的文件名
// 文件名：当前模块文件的绝对路径
// console.log(module.filename)

// module.loaded：属性值为布尔值，
// 当属性值为false时表示模块尚未加载完毕，
// 当属性值为true时表示模块加载完毕
// console.log(module.loaded)

// module.parent：当前模块的父模块对象，即调用当前模块的模块对象
// console.log(module.parent)

// module.children：属性值为一个数组，存放了当前模块的所有子模块对象，
// 即当前模块中已加载的所有模块对象